# レスポンシブ
```css
/*
スマホのレイアウト
まずはスマホを中心としてレイアウトを作成しましょう。
したがって、スマホのレイアウトに対してはメディアクエリを記述する必要はありません。
*/

// タブレットのレイアウト
@media (min-width: 768px) {
  // 横幅が768px以上の時のレイアウト
}

// ノートパソコンのレイアウト
@media (min-width: 992px) {
  // 横幅が992px以上の時のレイアウト
}

// デスクトップのレイアウト
@media (min-width: 1200px) {
  // 横幅が1200px以上の時のレイアウト
}
```